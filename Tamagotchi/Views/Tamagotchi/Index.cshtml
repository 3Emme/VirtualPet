@using Tamagotchi.Models;

<h1>Your Virtual Pets</h1>
@if (Model.Count == 0)
{
  <p>You don't have any pets yet!</p>
}
<ul>
  @foreach (VirtualPet virtualPet in Model)
  {
    <li><a href='tamagotchi/@virtualPet.Id'>@virtualPet.Name, @virtualPet.Breed</a></li>
  }
</ul>